extends ../responsive

include inc/message

block content
	include navbar
	
	.nspacer
	
	for message in messages
		+message(message)
	
	if (messages.length == 0)
		p.text-center.lead No Private Messages
	
	.clearfix